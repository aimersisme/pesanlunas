import { AppShell } from "@/components/app-shell";import { InvoiceDetail } from "@/components/modules/invoice-detail";import { getActiveBusiness } from "@/lib/business";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const[{id},b]=await Promise.all([params,getActiveBusiness()]);return <AppShell><InvoiceDetail invoiceId={id} businessId={b.id} role={b.role}/></AppShell>}
