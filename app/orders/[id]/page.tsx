import { AppShell } from "@/components/app-shell";import { OrderDetail } from "@/components/modules/order-detail";import { getActiveBusiness } from "@/lib/business";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const[{id},b]=await Promise.all([params,getActiveBusiness()]);return <AppShell><OrderDetail businessId={b.id} role={b.role} orderId={id}/></AppShell>}
