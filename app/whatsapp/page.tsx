import { AppShell } from "@/components/app-shell";import { WhatsAppSettings } from "@/components/modules/whatsapp-settings";import { getActiveBusiness } from "@/lib/business";export const dynamic="force-dynamic";export default async function Page(){const b=await getActiveBusiness();return <AppShell><WhatsAppSettings businessId={b.id} role={b.role} businessWhatsapp={b.whatsapp}/></AppShell>}
