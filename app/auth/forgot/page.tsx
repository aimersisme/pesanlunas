"use client";
import Link from"next/link";import{useMemo,useState}from"react";import{createClient}from"@/lib/supabase/browser";import{Brand}from"@/components/brand";
export default function ForgotPage(){const supabase=useMemo(()=>createClient(),[]);const[email,setEmail]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function send(e:React.FormEvent){e.preventDefault();setBusy(true);const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/update-password`});setBusy(false);setMsg(error?error.message:"Link reset password sudah dikirim. Cek email Anda.")}return <main className="authStage"><section className="authCard"><Brand showTagline/><div className="authIntro"><h1>Lupa Password</h1><p>Kami kirim link reset ke email akun Anda.</p></div><form className="formStack" onSubmit={send}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label>{msg?<div className="formMessage">{msg}</div>:null}<button className="primaryButton" disabled={busy}>{busy?"Mengirim...":"Kirim Link Reset"}</button></form><p className="authSwitch"><Link href="/auth/login">Kembali ke Login</Link></p></section></main>}
