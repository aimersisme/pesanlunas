import { PublicInvoice } from "@/components/modules/public-invoice";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{token:string}>}){const{token}=await params;return <PublicInvoice token={token}/>}
