"use client";
import{useCallback,useEffect,useMemo,useState}from"react";import{createClient}from"@/lib/supabase/browser";import{FormActions,ModuleHeader,Notice}from"@/components/crud-ui";
type Member={id:string;user_id:string;role:string;status:string;joined_at:string|null;profiles:unknown};type Profile={full_name?:string|null;phone?:string|null};function profile(v:unknown):Profile{if(Array.isArray(v))return(v[0]??{})as Profile;return(v??{})as Profile}
export function TeamManager({businessId,role}:{businessId:string;role:string}){const supabase=useMemo(()=>createClient(),[]);const[members,setMembers]=useState<Member[]>([]);const[email,setEmail]=useState("");const[inviteRole,setInviteRole]=useState("staff");const[link,setLink]=useState("");const[saving,setSaving]=useState(false);const[msg,setMsg]=useState<{kind:"success"|"error";text:string}|null>(null);const isOwner=role==="owner";const load=useCallback(async()=>{const{data,error}=await supabase.from("business_members").select("id,user_id,role,status,joined_at,profiles(full_name,phone)").eq("business_id",businessId).order("created_at");if(error)setMsg({kind:"error",text:error.message});setMembers((data??[])as unknown as Member[])},[businessId,supabase]);useEffect(()=>{void load()},[load]);async function invite(e:React.FormEvent){e.preventDefault();setSaving(true);const{data,error}=await supabase.rpc("create_business_invitation",{p_business_id:businessId,p_email:email,p_role:inviteRole});setSaving(false);if(error)return setMsg({kind:"error",text:`Jalankan SQL patch v0.2.0 dulu. ${error.message}`});const token=String((data as Record<string,unknown>)?.token??"");setLink(`${location.origin}/join?token=${encodeURIComponent(token)}`);setMsg({kind:"success",text:"Undangan dibuat. Kirim link ke anggota tim."});setEmail("")}async function updateMember(m:Member,changes:Record<string,unknown>){const{error}=await supabase.from("business_members").update(changes).eq("id",m.id);if(error)return setMsg({kind:"error",text:error.message});setMsg({kind:"success",text:"Akses anggota diperbarui."});await load()}return <><ModuleHeader title="Anggota Tim" subtitle="Owner, Admin, Staff, dan Finance"/>{msg?<Notice kind={msg.kind}>{msg.text}</Notice>:null}{isOwner?<form className="formPanel" onSubmit={invite}><h2>Undang Anggota</h2><div className="fieldGrid"><label className="formField">Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="formField">Role<select value={inviteRole} onChange={e=>setInviteRole(e.target.value)}><option value="admin">Admin</option><option value="staff">Staff</option><option value="finance">Finance</option></select></label></div><FormActions saving={saving} submitLabel="Buat Link Undangan"/>{link?<label className="formField" style={{marginTop:12}}>Link Undangan<input readOnly value={link} onFocus={e=>e.currentTarget.select()}/><span className="formHint">Berlaku 7 hari. Anggota harus login memakai email yang diundang.</span></label>:null}</form>:null}<div className="crudList">{members.map(m=>{const p=profile(m.profiles);const isMemberOwner=m.role==="owner";return <article className="crudCard" key={m.id}><div className="crudCardTop"><div className="crudCardTitle"><strong>{p.full_name||"Pengguna PesanLunas"}</strong><small>{p.phone||m.user_id}</small></div><span className={`statusBadge ${m.status==="active"?"green":"amber"}`}>{m.role} · {m.status}</span></div>{isOwner&&!isMemberOwner?<div className="crudCardActions"><select className="miniButton" value={m.role} onChange={e=>void updateMember(m,{role:e.target.value})}><option value="admin">Admin</option><option value="staff">Staff</option><option value="finance">Finance</option></select><button className={`miniButton ${m.status==="active"?"danger":"primary"}`} onClick={()=>void updateMember(m,{status:m.status==="active"?"suspended":"active"})}>{m.status==="active"?"Suspend":"Aktifkan"}</button></div>:null}</article>})}</div></>}
